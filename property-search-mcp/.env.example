PORT=3001
CORS_ORIGIN=http://localhost:5173

# Set to "true" to enable Asgardeo JWT verification + scope-based filtering.
# Leave unset/false when running behind an API gateway that handles auth
# (e.g. WSO2 Choreo). ASGARDEO_BASE_URL is only required when AUTH_ENABLED=true.
AUTH_ENABLED=false
ASGARDEO_BASE_URL=https://api.asgardeo.io/t/erandisaorg
